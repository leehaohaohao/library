<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lihao.dao.FeeCategoryDao">
    <select id="select" resultType="FeeCategory">
        select * from t_fee_category where fee_category_id = #{fee_category_id}
    </select>
</mapper>